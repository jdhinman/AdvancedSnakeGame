<?xml version="1.0" encoding="utf-8"?><!--
   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API Level 23; for those
   devices, auto-backup includes files in the app's private directory by default.
-->
<full-backup-content>
    <!-- TODO: Use <include> and <exclude> to control what is backed up.
        <include path="@string/my_data"/>
        <exclude path="@string/temporary_data"/>
    -->
</full-backup-content>